import styled from "styled-components";

const Wrapper = styled.div`
  width: var(--max-width);
  margin: 0 auto;
  padding: 3rem 0;
  p {
    line-height: 2;
    color: var(--grey-500);
    margin-top: 2rem;
  }
`;

export default Wrapper;
